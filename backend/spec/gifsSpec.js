const request = require('request');
const server = require('../bin/www');

const createGifEndpoint;

describe('Gifs Endpoints', function(){
    it('', function(done) {

        done();
    }, 20000);
});